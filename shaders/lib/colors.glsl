/* Colors */

#define rgb(x, y, z) vec3(x, y, z)

const vec3 zenith_color = rgb(67, 133, 208) / 255.0;
const vec3 horizon_color = rgb(148, 198, 234) / 255.0;
const vec3 void_color = rgb(255, 251, 161) / 255.0;

const vec3 night_zenith_color = rgb(38, 34, 123) / 255.0;
const vec3 night_horizon_color = rgb(60, 60, 166) / 255.0;
const vec3 night_void_color = rgb(61, 40, 91) / 255.0;

const vec3 light_source = rgb(255, 165, 67) / 255.0 ;
const vec3 light_destination = rgb(62, 62, 125) / 255.0;