/* Colors */

#define rgb(x, y, z) (vec3(x, y, z) / 255.0)

const vec3 zenith_color = rgb(67, 133, 208);
const vec3 horizon_color = rgb(148, 198, 234);
const vec3 void_color = rgb(255, 251, 161);

const vec3 night_zenith_color = rgb(38, 34, 123);
const vec3 night_horizon_color = rgb(60, 60, 166);
const vec3 night_void_color = rgb(61, 40, 91);

const vec3 light_source = rgb(255, 180, 0) * 1.2;
const vec3 light_destination = rgb(62, 90, 125);
const vec3 light_sky = rgb(250, 255, 200);